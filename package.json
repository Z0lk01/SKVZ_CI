{
  "devDependencies": {
    "cypress": "^12.8.1"
  },
  "dependencies": {
    "cypress-parallel": "^0.12.0"
  }
  ,"scripts": {
    "test": "cypress run --config-file cypress/cypress.json"
  }
  

}
